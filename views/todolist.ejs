<html>
  <head>
    <title>Todo List</title>
    <meta charset="UTF-8">
  </head>
  <body>
    <h1>Todo List</h1>

    <h3>Tasks</h3>
    <ul>
      <% tasks.forEach((task) => { %>
        <% if (task.done) { %>
          <li>
            <s><%= task.title %></s> 
            | <a href="#"> Delete</a>
          </li>
        <% } else { %>
            <li>
              <%= task.title %> 
              | <a href="#"> Done</a>
            </li>
        <% } %>
      <% }) %>
    </ul>
  </body>
</html>